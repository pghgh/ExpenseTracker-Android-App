<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.ac.univie.t0306.expensetracker">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTracker">
        <activity
            android:name=".ui.transaction.EditTransactionActivity"
            android:exported="true" />
        <activity
            android:name=".ui.accounts.EditAccountActivity"
            android:exported="true" />
        <activity
            android:name=".ui.categories.EditCategoryActivity"
            android:exported="true" />
        <activity
            android:name=".ui.categories.AddCategoryActivity"
            android:exported="true" />
        <activity
            android:name=".ui.accounts.AddAccountActivity"
            android:exported="true" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>